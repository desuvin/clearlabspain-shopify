{% comment %}
  Renders a collection card

  Accepts:
  - card_collection: {Object} Collection Liquid object
  - media_aspect_ratio: {String} Size of the product image card. Values are "square" and "portrait". Default is "square" (optional)
  - columns: {Number}
  - extend_height: {Boolean} Card height extends to available container space. Default: false (optional)
  - wrapper_class: {String} Wrapper class for card (optional)
  - placeholder_image: {String} Placeholder image when collection is empty (optional)

  Usage:
  {% render 'card-collection' %}
{% endcomment %}

{{ 'custom-card-collection.css' | asset_url | stylesheet_tag }}
<a href="/collections/{{ card_collection.handle }}">
<div class="product">
  <img src="{{ card_collection.metafields.custom.card_image.value | image_url }}" alt="Collection Name">
  <h3>{{ card_collection.metafields.custom.display_name }}</h3>
  <!-- <p>{{ card_collection.metafields.custom.card_image.value | image_url: width: 750 }}</p>
  {{ collection.image }} -->
</div>
</a>